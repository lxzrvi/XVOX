<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <!-- Background card -->
    <ImageView
        android:id="@+id/widget_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY" />

    <!-- Left Side: Cover Artwork -->
    <RelativeLayout
        android:id="@+id/widget_left_cover_container"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="10dp">

        <ImageView
            android:id="@+id/widget_cover"
            android:layout_width="60dp"
            android:layout_height="match_parent"
            android:scaleType="centerCrop" />

        <ImageView
            android:id="@+id/widget_fallback_logo"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_centerInParent="true"
            android:visibility="gone"
            android:src="@drawable/ic_xvox_music_note" />
    </RelativeLayout>

    <!-- Right Side: Music Controls -->
    <LinearLayout
        android:id="@+id/widget_right_controls"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/widget_btn_like"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:background="?android:selectableItemBackgroundBorderless"
            android:src="@drawable/ic_xvox_heart_outline"
            android:tint="#FFFFFF"
            android:scaleType="centerInside" />

        <ImageButton
            android:id="@+id/widget_btn_prev"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="6dp"
            android:background="?android:selectableItemBackgroundBorderless"
            android:src="@drawable/ic_xvox_skip_previous"
            android:tint="#FFFFFF"
            android:scaleType="centerInside" />

        <ImageButton
            android:id="@+id/widget_btn_play_pause"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginStart="6dp"
            android:layout_marginEnd="6dp"
            android:background="?android:selectableItemBackgroundBorderless"
            android:src="@drawable/ic_xvox_play"
            android:tint="#FFFFFF"
            android:scaleType="centerInside" />

        <ImageButton
            android:id="@+id/widget_btn_next"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?android:selectableItemBackgroundBorderless"
            android:src="@drawable/ic_xvox_skip_next"
            android:tint="#FFFFFF"
            android:scaleType="centerInside" />
    </LinearLayout>

    <!-- Center Info: Title & Artist -->
    <LinearLayout
        android:id="@+id/widget_center_info"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toEndOf="@+id/widget_left_cover_container"
        android:layout_toStartOf="@+id/widget_right_controls"
        android:layout_marginEnd="8dp"
        android:orientation="vertical"
        android:gravity="center_vertical">

        <!-- Cinzel X Logo -->
        <ImageView
            android:id="@+id/widget_logo_cinzel"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginBottom="2dp"
            android:scaleType="fitCenter" />

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="XVOX Music"
            android:textColor="#FFFFFF"
            android:textSize="13sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/widget_artist"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tap to play"
            android:textColor="#D0D0D0"
            android:textSize="11sp"
            android:maxLines="1"
            android:ellipsize="end" />
    </LinearLayout>

</RelativeLayout>
