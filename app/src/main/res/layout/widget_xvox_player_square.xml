<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Background card -->
    <ImageView
        android:id="@+id/widget_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY" />

    <!-- Full Bleed Cover Artwork -->
    <ImageView
        android:id="@+id/widget_cover"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop" />

    <!-- Fallback X logo when no cover -->
    <ImageView
        android:id="@+id/widget_fallback_logo"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_centerInParent="true"
        android:visibility="gone"
        android:src="@drawable/ic_xvox_music_note" />

    <!-- Bottom Controls with translucent dark backing -->
    <LinearLayout
        android:id="@+id/widget_controls_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#99000000"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="6dp"
        android:paddingBottom="8dp">

        <!-- Title & Artist -->
        <TextView
            android:id="@+id/widget_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="XVOX Music"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/widget_artist"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No track playing"
            android:textColor="#D0D0D0"
            android:textSize="10sp"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- Buttons Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="36dp"
            android:gravity="center"
            android:orientation="horizontal"
            android:layout_marginTop="3dp">

            <ImageButton
                android:id="@+id/widget_btn_like"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:background="?android:selectableItemBackgroundBorderless"
                android:src="@drawable/ic_xvox_heart_outline"
                android:tint="#FFFFFF"
                android:scaleType="centerInside" />

            <ImageButton
                android:id="@+id/widget_btn_prev"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginStart="8dp"
                android:background="?android:selectableItemBackgroundBorderless"
                android:src="@drawable/ic_xvox_skip_previous"
                android:tint="#FFFFFF"
                android:scaleType="centerInside" />

            <ImageButton
                android:id="@+id/widget_btn_play_pause"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:background="?android:selectableItemBackgroundBorderless"
                android:src="@drawable/ic_xvox_play"
                android:tint="#FFFFFF"
                android:scaleType="centerInside" />

            <ImageButton
                android:id="@+id/widget_btn_next"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginEnd="8dp"
                android:background="?android:selectableItemBackgroundBorderless"
                android:src="@drawable/ic_xvox_skip_next"
                android:tint="#FFFFFF"
                android:scaleType="centerInside" />
        </LinearLayout>
    </LinearLayout>

</RelativeLayout>
